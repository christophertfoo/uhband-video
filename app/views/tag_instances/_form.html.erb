<%= form_for(@tag_instance) do |f| %>
  <% if @tag_instance.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tag_instance.errors.count, "error") %> prohibited this tag_instance from being saved:</h2>

      <ul>
      <% @tag_instance.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :timestamp %><br>
    <%= f.number_field :timestamp %>
  </div>
  <div class="field">
    <%= f.label :media_id %><br>
    <%= f.number_field :media_id %>
  </div>
  <div class="field">
    <%= f.label :tags_id %><br>
    <%= f.number_field :tags_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
